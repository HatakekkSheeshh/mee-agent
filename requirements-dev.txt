# Dev / test dependencies for the meeting/ package suite (tests/meeting/).
# Install on top of requirements.txt:  venv/bin/pip install -r requirements-dev.txt
# Run:  venv/bin/python -m pytest        (pytest.ini scopes to tests/meeting)
pytest>=8,<10
pytest-asyncio>=0.23,<2
