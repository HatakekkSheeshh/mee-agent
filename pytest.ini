[pytest]
# Scope to the meeting/ package suite. The legacy tests/ files
# (test_server.py, test_client.py, test_vad.py, test_batch_inference.py)
# target the whisper_live streaming system and import whisper/jiwer — they
# are not part of this suite. Run them explicitly if needed.
testpaths = tests/meeting
asyncio_mode = auto
